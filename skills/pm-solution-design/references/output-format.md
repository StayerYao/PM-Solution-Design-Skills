# 输出格式规范

**标题层级：** `#` 文档标题 → `##` 主章节 → `###` 子主题 → `####` 细节分组

**核心格式规则：**
- **加粗** 标注关键结论、核心指标、重要约束
- `>` 引用块 + emoji + **加粗标签** 构成 callout
- `- [ ]` 复选框用于验收标准，表格用于对比和维度展开
- `` 行内代码 `` 用于字段名、状态值、规则表达式

**Callout 约定：**

| 标签 | 用途 |
|------|------|
| `📌 关键决策` | 不可逆的重大选择，需干系人确认 |
| `⚠️ 风险提示` | 已知风险、依赖阻塞点 |
| `💡 设计思路` | WHY 的解释 |
| `🔴 约束` | 绝对不能做的约束 |
| `✅ 验收重点` | 验收时需特别关注的核心项 |
| `⚡ 灵感来源` | 来自灵感碰撞的设计，标注 PM 输入 |
| `⚖️ 取舍声明` | 重大取舍的四元组声明 |

**需求级别：** `[P0]` 阻塞上线 / `[P1]` 本期高优 / `[P2]` 时间允许则纳入
