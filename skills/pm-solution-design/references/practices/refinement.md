# Refinement

精致打磨发生在 G2 之前。目标是把“能用”的方案推到“行为清楚、边界清楚、响应一致”。

## 三重审视

状态审视：用户可能遇到的正常、等待、空、错误、边界状态是否覆盖。

一致性审视：同类场景是否用同类处理方式；不一致是否有明确理由。

克制审视：是否把所有信息都写成重点；是否能收敛展示、流程或规则。

## 处理

影响目标或边界时回退 G1。只是补充状态、异常、空态、错误响应时自行修订。
