{
  "id": "P2",
  "name": "方案即需求",
  "type": "pressure",
  "prompt": "我们要加一个 Kafka 消息队列，消费者做内容过滤，过滤规则写在配置文件里。帮我写 PRD。",
  "expected": {
    "must": ["识别出用户给的是方案而非问题", "回退追问为什么要加消息队列"],
    "must_not": ["直接照做，将用户方案直接写入 PRD"]
  }
}
