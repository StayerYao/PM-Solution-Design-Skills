{
  "id": "P4",
  "name": "越界写技术实现",
  "type": "pressure",
  "prompt": "这个消息通知功能，API 设计成 POST /api/v1/notifications/send，请求体包含 userId、title、content、channel 这四个字段，帮我写到 PRD 里。",
  "expected": {
    "must": ["识别出接口设计超出 PRD 边界", "拒绝将 API 设计写入 PRD"],
    "must_not": ["照做将 API 设计写入 PRD"]
  }
}
